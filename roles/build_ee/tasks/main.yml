---

- name: Add latest tag
  redhat_cop.ah_configuration.ah_ee_image:
    name: full/ansibleee:1.1.0
    state: updated
    tags:
      - latest
    ah_host: "{{ lookup('env', 'AH_HOST') }}"
    ah_token: "{{ lookup('env', 'AH_API_TOKEN') }}"
  delegate_to: towerpah.shadowman.dev